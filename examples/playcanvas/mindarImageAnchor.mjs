import { Script } from 'playcanvas';

export class MindarImageAnchor extends Script {
  static scriptName = 'mindarImageAnchor';

  /**
   * Target index inside the .mind file.
   * @attribute
   * @title Target Index
   */
  targetIndex = 0;
}
